# ── 데이터 원본·산출물 (용량·기사 원문 저작권 — 저장소에 포함하지 않음) ──
news.csv
articles.xlsx
*.xlsx
~$*
data/

# ── Python ──
__pycache__/
*.py[cod]
venv/
.venv/
.ipynb_checkpoints/

# ── 비밀키·환경변수 (CLOVA·KOSIS API 키 — 절대 커밋 금지) ──
.env
.env.*
!.env.example          # 변수명 정본은 커밋한다(값은 비어 있음)

# ── 캐시(LLM record-replay — 응답 본문이라 저장소에 넣지 않는다) ──
cache/

# ── OS·에디터 ──
Thumbs.db
.DS_Store
